# Processor — PyFlink stream processing (dedup, filter, route)
# Note: apache-flink is installed inside the Flink Docker image.
# This file lists the Python dependencies bundled into the job JAR.
apache-flink==1.18.1
apache-flink-libraries==1.18.1
pydantic==2.6.1
